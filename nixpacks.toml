[phases.setup]
nixPkgs = [
  "python311",
  "google-chrome",
  "chromedriver",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn server:app --workers 1 --timeout 300 --bind 0.0.0.0:$PORT"

