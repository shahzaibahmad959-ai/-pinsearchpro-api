[phases.setup]
nixPkgs = [
  "chromium",
  "chromedriver",
  "python311",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn server:app --workers 2 --timeout 300 --bind 0.0.0.0:$PORT"
